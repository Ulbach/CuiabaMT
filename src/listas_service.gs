/** 
 * listas_service.gs
 *
 * Serviço desativado propositalmente.
 * As funções de lista foram embutidas em Code.gs para evitar conflito/duplicidade
 * e erro de função não definida no Apps Script.
 *
 * Mantenha este arquivo no projeto para compatibilidade estrutural,
 * mas não declare funções aqui.
 */
